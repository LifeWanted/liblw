# TODO: Add `-Werror` once the GCC bugs are fixed...
build --cxxopt='-std=c++20' --cxxopt='-Wall' --cxxopt='-fdiagnostics-color=always' --cxxopt='-fcoroutines'
test --test_output=errors --compilation_mode=dbg
